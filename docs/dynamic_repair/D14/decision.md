# D14 — decision (docs / report / README reconciliation — the FINAL stage)

**Result: KEEP. The docs now match the source + the D13 results; the stale claims that launched this
campaign are corrected. The D0–D14 dynamic-repair campaign is COMPLETE.** Pure docs — no `src`/test
changes; suite **706 unit / 63 contract, 0 failed**.

## What changed (4 docs, claims reconciled to code/results)
1. **`result_save/DYNAMIC_TASK_REPORT.md`** — prepended a prominent **SUPERSEDED correction banner** (the
   frozen report is preserved as history, not deleted). It corrects the three claims the campaign was
   launched to fix: (a) the frozen data was **single-RSU random geometry**, not a 4-RSU urban grid — the
   "4 RSU / urban v2x_37885" was the channel **regime**, not the scene geometry (real 4-RSU urban arrived
   in D1); (b) "full (recurrent) model" = **recurrent-vs-memoryless ONLY** (COMA/SCQ/chance/CVaR/Pareto/PNA
   were not in the dynamic branch then — D9–D12 added them, D13 A/B'd them, all CIs span 0); (c) the
   pipeline it measured was itself corrected (D2–D7). Net: the report's central conclusion **survives and
   is strengthened**, but its data description + "full model" scope were wrong → cite D13.
2. **`docs/CURRENT_HEAD_STATUS.md`** — prepended a **DYNAMIC-REPAIR CAMPAIGN COMPLETE** banner (above the
   v2 banner): pipeline corrections, real 4-RSU urban data, all mechanisms opt-in, the D13 headline numbers,
   the recommended dynamic config.
3. **`README.md`** — rewritten from the retired **"critic-free is the project identity"** framing to
   **CTDE** (central critic legal in training; decentralized deployment is the binding invariant), matching
   `AGENTS.md`. Recommended arm `--baseline graph-mappo --actor mlp`; the dynamic arm + `--dyn-data urban`
   documented; the stale headlines (0.610-vs-0.655, N=24 +0.177) marked retired/superseded; invariant I1
   fixed from "no central critic" to deployment-decentralization; "critic-free" now names only the retained
   REINFORCE **baseline**, not the identity.
4. **`docs/URBAN_V2X_RESEARCH_LOG.md`** — appended a consolidated **DYNAMIC-REPAIR CAMPAIGN SUMMARY
   (D0–D14)**: the 10-gap list, a stage ledger (what / commit / verified / result), the D13 central result,
   and a dynamic mechanism ledger (each flag, budget, D13 paired Δfeas) — mirroring the v2 SUMMARY.

## Adversarial verification (claims-vs-evidence, focused agent, 5 items A–E, all PASS, no blockers)
- **A** frozen-report correction is accurate vs source (single-RSU `_sample_scene`→`_node`; 4-RSU
  `build_urban_grid_scene` only in D1's `sample_dynamic_urban_scenes`; "full model" = recurrent-vs-memoryless
  only; central conclusion honestly survives, no overclaimed reversal).
- **B** status + research-log numbers **exact-match** `dynamic_d13_campaign.json` (learned 0.151/0.196,
  deployable 0.364/0.369 random + 0.696/0.711 urban, central 0.374/0.793, PNA +0.125 [−0.297,+0.547], SCQ
  43/upd, Pareto 144); stage-ledger commit hashes match `git log`.
- **C** README is accurate CTDE; every cited flag (`--baseline graph-mappo`, `--actor mlp`, `--dynamic`,
  `--dyn-data urban`) exists in the trunk; retired headlines correctly marked.
- **D** no live critic-free-identity residue (it is named only as retired history / the retained baseline).
- **E** scope honest in all four docs (single-RSU random + 4-RSU urban, N≤16, 5 seeds; NOT at-scale /
  N≥24); the "deployable ≈ central" caveat (tight on random, loose on urban) is stated.

## Campaign close-out
- **D0 · D2 · D3 · D4 · D5 · D6 · D7 · D8 · D9 · D10 · D11 · D12 · D1 · D13 · D14 — ALL DONE. All 10 gaps
  closed.** Every stage adversarially verified (D4 caught a real BLOCKER; D8/D13 conclusions re-derived
  from raw JSON; D1/D13 urban data hash regenerated byte-identically).
- **Central result:** the corrected, genuinely-4-RSU-urban dynamic task confirms the v2 pattern — every
  sophisticated mechanism (temporal/COMA/SCQ/chance/Pareto/PNA) is verified-correct but gives no headline
  gain; the simple baseline + torch-free local decoder is the default; the binding limit is RL
  feasibility-region learning. Open frontier (deferred): large-N (N≥24) generalization.
- Branch `decentralized-marl-trunk` is ahead of origin (unpushed). **Push is the owner's decision** — to be
  surfaced at this milestone.
