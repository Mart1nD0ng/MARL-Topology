# T7 — decision (docs close-out, pure docs) — CAMPAIGN COMPLETE

**Result: the Temporal-Recovery campaign (T0–T7) is COMPLETE.** T7 is a pure-docs close-out — no code, no new
experiments; the suite is unchanged (843/0). It records the campaign's central result and refined binding-limit
diagnosis across the repo's authority docs.

## What T7 wrote (docs only)
- `docs/CURRENT_TEMPORAL_RECOVERY_STATUS.md` — a **🏁 CAMPAIGN COMPLETE** banner (top) with the refined diagnosis;
  the §5 per-stage ledger (T0–T6) is already present.
- `docs/CURRENT_HEAD_STATUS.md` — a **🏁 TEMPORAL-RECOVERY COMPLETE** banner atop (above the Belief-Residual one);
  the 5th honest negative.
- `docs/URBAN_V2X_RESEARCH_LOG.md` — the **TEMPORAL-RECOVERY CAMPAIGN SUMMARY** (tail): per-stage ledger T0–T7
  with commit hashes, the central result, the refined diagnosis, and the per-stage Workflow IDs.
- `AGENTS.md` — the Temporal-Recovery campaign as the **5th** bullet in the superseding-campaigns ledger; "FOUR"
  → "FIVE" campaigns; the open-frontier updated with the task-1 env-feature modification.
- `docs/temporal_recovery/T7/decision.md` — this file.

## Central result
**No temporal module beats the stale `local_hysteresis` anchor at N≤16.** The stale-CSI headroom is REAL (T1
oracle: perfect/true-CSI recovery converts, urban +0.165), and the current channel is PARTIALLY recoverable from
the leak-free features (T6: the stale history adds recoverable R² beyond geometry), but no realizable temporal
mechanism CONVERTS the recovered channel into a deployed feasibility gain. **Binding limit = the deployable
PRECISION of the recovered channel at the anchor's decision boundary** — realizable predictions beat the stale
echo *on average* (R²/MSE) but are not precise enough at the decision-critical edges near the keep/add
thresholds. This is the project's recurring "deployable precision at N≤16" wall (5th independent campaign), now
localized to the temporal/CSI-recovery axis with the sharpest mechanistic story yet.

## Per-stage disposition (all opt-in / default-off, adversarially verified)
- T1 oracle GATE PASS (headroom real; direct-`p_t` harmful; correction essential; MSE ⟂ decisions).
- T2 leaky-tanh activation — KEEP (enabling; byte-identical off).
- T3 correction target — KEEP parametrization / HONEST NEGATIVE (direction learnable, magnitude not).
- T4 edge recurrence — KEEP opt-in / HONEST NEGATIVE (locus is not the limit; more capacity → worse decisions).
- T5 uncertainty-gating — KEEP opt-in / HONEST NEGATIVE (uncertainty weakly calibrated).
- T6 env temporal-structure diagnostic — REFINES the limit (partially recoverable, precision-limited).

## Provenance / honesty
Every stage T1–T6 carries a Claim Card + Mechanism-Path Matrix + failing-first load-bearing test +
Effect-on-Decision report + a path-specific negative + a per-stage adversarial multi-lens Workflow (all PASS
0-MAJOR, leak-free, byte-identical-off, with independent reproductions). All 5-seed results have CIs; no headline
rests on a pilot or a span-0 mean (the T3 move-weight backfire is explicitly n=1). The T3–T5 interim "magnitude
not in the features" wording is superseded by the T6-refined "recoverable-but-precision-limited" diagnosis.
Commits T0–T7 on `decentralized-marl-trunk`, **NOT pushed** — the push is the owner's decision (asked at
close-out).

## Decision
**CAMPAIGN COMPLETE.** The push decision is deferred to the owner (AskUser). Open frontier: the task-1 env
MODIFICATION (give the production channel's decision-critical component temporal autocorrelation, motivated by
T6's synthetic sweep, and re-run the oracle for feasibility conversion); a higher-precision recovered signal at
the decision boundary; larger-N (N≥24) with a cheaper exact-fault evaluator.
